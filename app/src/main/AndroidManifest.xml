<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="me.lamprosgk.milkyway">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".MilkyWayApplication"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            tools:ignore="GoogleAppIndexingWarning">
        <activity
                android:name="me.lamprosgk.milkyway.ui.detail.MilkyDetailActivity"
                android:label="@string/title_activity_milky_detail"
                android:theme="@style/AppTheme.NoActionBar"
                android:parentActivityName="me.lamprosgk.milkyway.ui.milkies.MilkiesActivity">
        </activity>
        <activity android:name="me.lamprosgk.milkyway.ui.milkies.MilkiesActivity"
                  android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>